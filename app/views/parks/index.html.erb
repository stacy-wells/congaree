<div class="index">
  <div class="index__wrapper">
    <% @parks.each do |park| %>
      <div class="index__item">
        <div class="index__item-photo"></div>
        <a href="<%= park_path(park) %>"><%= park.name %></a>
        <p><%= park.state %></p>
      </div>
    <% end %>
  </div>
</div>
